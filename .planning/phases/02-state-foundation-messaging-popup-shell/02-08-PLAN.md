---
phase: 02-state-foundation-messaging-popup-shell
plan: 08
type: execute
wave: 5
depends_on:
  - 02-03
  - 02-04
  - 02-05
  - 02-06
  - 02-07
files_modified: []
autonomous: false
requirements:
  - ENG-04
  - ENG-06
  - UI-01
  - UI-03
  - UI-04
  - UI-08
  - BLD-03
  - BLD-04
  - MAN-03
  - MAN-04
user_setup: []

must_haves:
  truths:
    - "`pnpm build` succeeds and emits `apps/extension/.output/chrome-mv3/registry.json` (BLD-03 visible artifact)."
    - "`apps/extension/.output/chrome-mv3/chunks/experiments-andrew__smoke-*.js` exists (BLD-04 visible artifact)."
    - "Loaded into Chrome via 'Load unpacked', the popup renders the shadcn shell (header `Experiments` + count Badge + author group `andrew (1)` + Card with `Smoke pink` name + status Badge + Switch)."
    - "Toggling the Switch ON applies the smoke on `https://ya.ru/` (page goes pink) and OFF reverts (page goes white) — without page reload (Roadmap Phase 2 success criterion #1 + #2)."
    - "Toggle persists across SW termination (Roadmap success #2): `chrome://serviceworker-internals` Stop → re-open popup → Switch is still ON → page is still pink (or re-pinks on reconcile)."
    - "Forcing 3 errors in 10s (e.g., editing the smoke to `throw` on apply) trips `auto-disabled`: popup shows red-outline `Auto-disabled` Badge + `Disabled after 3 errors in 10s — toggle off/on to re-enable.` (Roadmap success #4)."
    - "Toggle OFF→ON re-arms (D-13): smoke applies again (or fails again, fresh window)."
    - "All 5 status badge variants render correctly with documented color + label (UI-04 / D-23)."
    - "Click `Show stack trace` expands inline Collapsible with monospace stack (UI-08)."
    - "All Phase 1 invariants still hold: SW survives idle restart with stateless storage; CSP guardrail blocks eval/new Function/remote import (`pnpm check-csp` exits 0)."
  artifacts:
    - path: ".planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md"
      provides: "Checkpoint record: which manual checklist items passed; deferred items called out for /gsd-verify-work"
      contains: "Browser-Manual MV3 Smoke"
  key_links:
    - from: "Phase 2 plans 02-01..02-07"
      to: "Phase 2 verification (next: /gsd-verify-work)"
      via: "manual smoke checklist documented + auto-mode disposition recorded"
      pattern: "Browser-Manual"
---

<objective>
Verify the integrated Phase 2 system end-to-end on Chromium MV3 via the manual smoke checklist documented in 02-RESEARCH.md §12 + 02-VALIDATION.md "Manual-Only Verifications". This plan does NOT modify code; it produces a checkpoint record (`02-MANUAL-SMOKE.md`) that captures the literal browser observations the verifier and `/gsd-verify-work` need to close Phase 2's Roadmap success criteria #1, #2, #4.

Purpose: Phase 2 has automated coverage for storage/messaging/handler logic + RTL coverage for the popup. The 5-state-badge visual render, toggle-persists-across-SW-restart, 3-errors-in-10s trip + visible reason, registry.json artifact presence, and toggle-revert behavior require literal browser interaction — they cannot be replaced by automated tests (RESEARCH §12 A3).

Output: 1 markdown file recording the smoke session (or, under chained auto-mode per Plan 01-05's precedent, an auto-approved disposition with the durable record deferred to Andrew's local Chrome session).
</objective>

<execution_context>
@/Users/andreas/Library/Mobile Documents/com~apple~CloudDocs/Code/gsd-test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreas/Library/Mobile Documents/com~apple~CloudDocs/Code/gsd-test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-foundation-messaging-popup-shell/02-CONTEXT.md
@.planning/phases/02-state-foundation-messaging-popup-shell/02-RESEARCH.md
@.planning/phases/02-state-foundation-messaging-popup-shell/02-VALIDATION.md
@.planning/phases/02-state-foundation-messaging-popup-shell/02-UI-SPEC.md
@.planning/phases/02-03-SUMMARY.md
@.planning/phases/02-04-SUMMARY.md
@.planning/phases/02-05-SUMMARY.md
@.planning/phases/02-06-SUMMARY.md
@.planning/phases/02-07-SUMMARY.md
@.planning/phases/01-foundation-spike-engine-skeleton/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite + build; verify artifact presence</name>
  <files>.planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md</files>
  <read_first>
    - .planning/phases/02-state-foundation-messaging-popup-shell/02-VALIDATION.md (full automated + manual gate list)
    - .planning/phases/02-state-foundation-messaging-popup-shell/02-RESEARCH.md §12 "Browser-Manual MV3 Smoke" (5 numbered checks)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-VERIFICATION.md (Phase 1 closure pattern — analog for the smoke record format)
  </read_first>
  <action>
    Run the full automated suite (PER 02-VALIDATION.md "Full suite command") from repo root:

    ```bash
    pnpm test --run
    pnpm typecheck
    pnpm lint
    pnpm check-csp
    pnpm build
    ```

    All 5 commands MUST exit 0. If any fails, STOP and report the failure — do not proceed to the manual smoke.

    After `pnpm build`:

    ```bash
    # Artifact presence (BLD-03 + BLD-04)
    ls -la apps/extension/.output/chrome-mv3/registry.json
    cat apps/extension/.output/chrome-mv3/registry.json | head -40
    ls -la apps/extension/.output/chrome-mv3/chunks/ | grep "experiments-"
    ```

    Both must succeed. Capture sample output (the first 40 lines of registry.json + the chunks/ listing) to embed in the smoke record.

    Create `.planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` with this initial structure:

    ```markdown
    ---
    phase: 02-state-foundation-messaging-popup-shell
    smoke_date: <YYYY-MM-DD>
    auto_mode_chained: <true|false>
    operator: andrew
    ---

    # Phase 2 — Manual MV3 Smoke Record

    ## Automated suite results

    | Command | Exit | Notes |
    |---------|------|-------|
    | `pnpm test --run` | <0/N> | <pasted summary line: total tests/passed> |
    | `pnpm typecheck` | <0/N> | |
    | `pnpm lint` | <0/N> | |
    | `pnpm check-csp` | <0/N> | |
    | `pnpm build` | <0/N> | <output dir + size> |

    ## Build artifacts (BLD-03 + BLD-04)

    `apps/extension/.output/chrome-mv3/registry.json` (BLD-03):

    ```json
    <paste first 40 lines of registry.json here>
    ```

    `apps/extension/.output/chrome-mv3/chunks/` (BLD-04):

    ```
    <paste output of: ls -la apps/extension/.output/chrome-mv3/chunks/ | grep "experiments-">
    ```

    ## Browser-manual checklist (RESEARCH §12)

    ### 1. 5-state badge render — UI-04 / D-23

    Status: ⬜ pending

    Test:
      1. `pnpm build`, load `apps/extension/.output/chrome-mv3/` via Chrome `Load unpacked`.
      2. Open the popup with the smoke experiment present (Phase 1 artifact at `experiments/andrew/smoke/`).
      3. Inject 4 fixture entries via the popup devtools console (or temporarily expand the smoke to throw on apply for the `error` + `auto-disabled` states).
      4. Confirm each Badge variant renders:
         - `Off` (gray secondary) — toggle OFF.
         - `Pending` (gray outline) — toggle ON, current tab not ya.ru.
         - `Applied` (green default) — toggle ON, on ya.ru, apply succeeded.
         - `Error` (red destructive) — toggle ON, last apply threw, count<3.
         - `Auto-disabled` (red outline override) — count≥3 in 10s.

    ### 2. Toggle persists across SW restart — Roadmap success #2

    Status: ⬜ pending

    Test:
      1. On `https://ya.ru/`, toggle smoke ON → page goes pink.
      2. Open `chrome://serviceworker-internals`, click `Stop` on the extension SW.
      3. Wait 2 seconds.
      4. Re-open the popup. Confirm the Switch is still ON. The page is still pink (or re-pinks on next reconcile).

    ### 3. 3-errors-in-10s trip + visible reason — Roadmap success #4 (ENG-06)

    Status: ⬜ pending

    Test:
      1. Edit `experiments/andrew/smoke/experiment.ts` to throw `new Error('test trip')` on apply, rebuild.
      2. Reload the extension in `chrome://extensions`.
      3. On ya.ru, toggle smoke ON → tab attempts apply, fails.
      4. Reload the tab 2 more times within 10 seconds → 3 errors logged.
      5. Open popup. Confirm:
         - Badge shows red-outline `Auto-disabled`.
         - Inline message: `Disabled after 3 errors in 10s — toggle off/on to re-enable.`
         - Below: `Last error: Error: test trip`.
         - `Show stack trace` clickable expands a monospace panel with the stack.
      6. Toggle OFF → toggle ON. Confirm:
         - Badge resets to `Error` or `Applied` depending on the next apply outcome.
         - Auto-disable banner disappears.
         - `error_window:<id>` is fresh (counter starts from 0 per D-13 recovery).
      7. Restore the smoke `experiment.ts` and rebuild before continuing other smokes.

    ### 4. registry.json artifact present — BLD-03

    Status: ⬜ pending

    Test:
      1. `cat apps/extension/.output/chrome-mv3/registry.json`
      2. Confirm valid JSON array; each entry has all 9 fields (`id, author, folder, name, description, scope, world, chunkPath, tweaks`).
      3. Confirm `chunkPath` resolves to a real file under `chunks/experiments-<author>__<folder>-<hash>.js`.

    ### 5. Toggle revert on ya.ru — Roadmap success #2

    Status: ⬜ pending

    Test:
      1. With smoke pinking ya.ru, toggle OFF in popup.
      2. Page reverts to white within ~1 second.
      3. Popup status badge changes from green `Applied` to gray `Off`.

    ## Auto-mode disposition

    `workflow._auto_chain_active = true` per `.planning/config.json:39`. Per Plan 01-05 precedent, when chained-run hits a manual smoke checkpoint, log `⚡ Auto-approved checkpoint`, treat resume signal as `approved`, record disposition here, continue.

    Disposition: <pasted by executor — either "Andrew completed all 5 checks on <date> with results <listed>" OR "Auto-approved under chained-run; durable record deferred to Andrew's local Chrome session per 01-05 precedent">.

    ## Sign-off for /gsd-verify-work

    - [ ] All 5 manual checks marked ✅ (or auto-approved disposition recorded with explicit reference to 01-05 precedent)
    - [ ] Automated suite all green
    - [ ] registry.json + chunks artifacts present
    - [ ] No regressions vs Phase 1 invariants (SW idle survival, CSP guardrail, error isolation, two-world routing)
    ```

    The executor (Claude) fills in the table values from the actual command output, and either:
    - **Manual mode:** records each check status `✅ pass` / `❌ fail` after Andrew runs the checklist, OR
    - **Chained auto-mode:** records `⚡ Auto-approved (chained-run)` per Plan 01-05 precedent and notes that Andrew's local Chrome session is the durable verification record.
  </action>
  <verify>
    <automated>pnpm test --run && pnpm typecheck && pnpm lint && pnpm check-csp && pnpm build && test -f apps/extension/.output/chrome-mv3/registry.json && test -d apps/extension/.output/chrome-mv3/chunks && ls apps/extension/.output/chrome-mv3/chunks/ | grep -E "experiments-andrew__smoke-" && test -f .planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` exists
    - `grep -E "## Automated suite results" .planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` returns 1
    - `grep -E "## Build artifacts" .planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` returns 1
    - `grep -E "## Browser-manual checklist" .planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` returns 1
    - `grep -cE "^### [1-5]\\." .planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` returns 5 (one per RESEARCH §12 numbered check)
    - `grep -E "Auto-disabled" .planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` returns at least 1 (Check 3 + Check 1 mention)
    - All 5 automated commands `pnpm test --run`, `pnpm typecheck`, `pnpm lint`, `pnpm check-csp`, `pnpm build` exit 0
    - `apps/extension/.output/chrome-mv3/registry.json` exists and parses as JSON array
    - `apps/extension/.output/chrome-mv3/chunks/experiments-andrew__smoke-*.js` exists
  </acceptance_criteria>
  <done>02-MANUAL-SMOKE.md created with automated-suite results + build-artifact captures + 5-check checklist; full suite + build artifacts verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human (or chained-auto disposition) confirms 5 manual MV3 smoke checks</name>
  <what-built>
    Phase 2 plans 02-01..02-07 shipped the typed messaging bus, SW orchestrator with auto-disable, popup shell, build artifacts (registry.json + per-experiment chunks), and content-script reconcile pipeline. Task 1 of this plan ran the full automated suite + build, confirmed artifact presence, and produced the 02-MANUAL-SMOKE.md record skeleton.
  </what-built>
  <how-to-verify>
    Walk the 5-check sequence in `.planning/phases/02-state-foundation-messaging-popup-shell/02-MANUAL-SMOKE.md` against your local Chrome:

    1. **5-state badge render (UI-04 / D-23):** Build, load `apps/extension/.output/chrome-mv3/` via "Load unpacked", open popup. Confirm each of the 5 Badge variants renders with the correct color + label (Off gray, Pending outline, Applied green, Error red solid, Auto-disabled red outline).

    2. **Toggle persists across SW restart (Roadmap success #2):** On ya.ru, smoke pink, `chrome://serviceworker-internals` → Stop → wait 2s → re-open popup. Switch still ON; page still pink.

    3. **3-errors-in-10s trip (ENG-06 / Roadmap success #4):** Patch smoke to throw on apply, force 3 errors in <10s, confirm `Auto-disabled` badge + `Disabled after 3 errors in 10s — toggle off/on to re-enable.` line + `Show stack trace` expand. Toggle OFF→ON re-arms.

    4. **registry.json artifact (BLD-03):** `cat apps/extension/.output/chrome-mv3/registry.json` shows valid JSON array, all 9 fields per entry (`id, author, folder, name, description, scope, world, chunkPath, tweaks`), `chunkPath` points to a real file.

    5. **Toggle revert on ya.ru (Roadmap success #2):** Toggle OFF → page goes white within ~1s; Badge changes from `Applied` to `Off`.

    For each: mark `✅ pass` / `❌ fail` (with notes) in 02-MANUAL-SMOKE.md.

    **Auto-mode disposition (Plan 01-05 precedent):** `workflow._auto_chain_active = true` is set in `.planning/config.json:39`. If running under chained-run AND no human is available to walk the smoke, follow Plan 01-05's documented precedent: log `⚡ Auto-approved checkpoint`, record `Auto-approved (chained-run); durable verification deferred to Andrew's local Chrome session` in the smoke doc, treat resume signal as `approved`, continue. The `/gsd-verify-work` step then becomes the literal verifier, and Andrew's local smoke is the durable record (same posture as Phase 1 closure 2026-04-25).
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass (manually) OR if auto-mode disposition is recorded per 01-05 precedent. Type "fail: <which check> <description>" to surface a regression for the executor to fix in a follow-up plan or commit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built extension → Chrome (Load unpacked) | Designer's own machine; no remote distribution in v1. |
| Manual smoke operator → smoke record file | Andrew records pass/fail; the record is reviewed at /gsd-verify-work time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-27 | Repudiation | Smoke record auto-approved without literal browser walk | mitigate | The Plan 01-05 precedent makes the disposition explicit AND points the durable record to Andrew's local Chrome. /gsd-verify-work cross-references the disposition; if any divergence appears in a later session, the disposition is invalidated and the smoke must be re-run before phase close. |
| T-2-28 | Tampering | Bug masked by smoke record because operator skipped a check | accept | Closed-team trust; Andrew is both operator and consumer. The 02-VALIDATION.md gate-list is exhaustive; any skipped check is recorded explicitly in 02-MANUAL-SMOKE.md as a TODO for /gsd-verify-work. |

ASVS L1: no `high` severity threats.
</threat_model>

<verification>
- All 5 automated suite commands (`pnpm test --run`, `pnpm typecheck`, `pnpm lint`, `pnpm check-csp`, `pnpm build`) exit 0.
- `apps/extension/.output/chrome-mv3/registry.json` exists with the 9 RegistryEntry fields per entry (`id, author, folder, name, description, scope, world, chunkPath, tweaks`).
- `apps/extension/.output/chrome-mv3/chunks/experiments-andrew__smoke-*.js` exists.
- `02-MANUAL-SMOKE.md` records the 5 manual checks with either pass/fail markers OR a chained-auto disposition explicitly referencing Plan 01-05.
- No regression vs Phase 1 invariants (SW idle survival via top-level listeners, CSP guardrail, error isolation, two-world routing).
</verification>

<success_criteria>
- 02-MANUAL-SMOKE.md exists with: automated suite table, build-artifact captures, 5 manual checks, sign-off list.
- All 5 checks have a status (✅ / ❌ / ⚡ Auto-approved).
- /gsd-verify-work has enough material to close Phase 2 against Roadmap success criteria #1, #2, #4 (the visual + toggle-persists + auto-disable-trip checks; criterion #3 (stable ULID + SDK types) is closed by automated tests in 02-01 + 02-02; criterion #5 (registry.json + chunks) is closed by the build-artifact section).
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-foundation-messaging-popup-shell/02-08-SUMMARY.md` documenting:
- Pasted summary of 02-MANUAL-SMOKE.md sign-off (which 5 checks passed; which were auto-approved per 01-05 precedent).
- Confirmation that all 5 automated commands exited 0.
- Pasted first 40 lines of `apps/extension/.output/chrome-mv3/registry.json` (BLD-03 visible artifact).
- Pasted output of `ls apps/extension/.output/chrome-mv3/chunks/` (BLD-04 visible artifact).
- Confirmation that Roadmap Phase 2 success criteria #1-5 are coverable by /gsd-verify-work using the smoke + automated coverage in 02-01..02-07.

## DEVIATIONS

(Expected: chained auto-mode disposition per Plan 01-05 precedent if no human walks the smoke. Record any failed checks here for follow-up plans.)
</output>
</output>
