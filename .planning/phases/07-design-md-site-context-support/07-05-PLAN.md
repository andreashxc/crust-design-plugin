# Plan 07-05: Validation, Privacy Audit, And Manual Smoke

## Goal

Verify Phase 7 end-to-end with a real local ya.ru `DESIGN.md` without publishing the private file.

## Tasks

- Run automated tests, typecheck, lint, CSP check, and extension build.
- Verify ignored context files do not appear in `git status`, `registry.json`, or `.output/chrome-mv3`.
- Create a local ya.ru experiment using the context-assisted prompt flow.
- Manually inspect that the resulting experiment follows target-site typography, spacing, component structure, and DOM-safe extension patterns.
- Document the workflow in `07-MANUAL-SMOKE.md`.

## Acceptance Criteria

- Full automated gate passes.
- Privacy audit confirms local context is not published or bundled.
- Manual smoke records before/after evidence for a context-assisted experiment.

