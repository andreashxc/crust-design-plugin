---
phase: 03-tweak-system
plan: 03-05
type: implementation
wave: 4
depends_on: [03-01, 03-04]
requirements: [UI-02, TWK-01, TWK-06]
files_modified:
  - experiments/andrew/tweak-demo/manifest.json
  - experiments/andrew/tweak-demo/experiment.ts
  - experiments/andrew/shadow-panel/manifest.json
  - experiments/andrew/shadow-panel/experiment.ts
  - apps/extension/src/content/shadow-dom.test.ts
  - tools/build-experiments.test.ts
autonomous: true
---

<objective>
Add reference experiments proving the Phase 3 user journey: a tweak-driven experiment with 3+ tweak types and a Shadow DOM in-page panel experiment.
</objective>

<tasks>

## Task 1: Add Tweak Demo Experiment

Files:

- `experiments/andrew/tweak-demo/manifest.json`
- `experiments/andrew/tweak-demo/experiment.ts`

Action:

- Create a ya.ru-scoped isolated-world experiment with at least:
  - `toggle`
  - `select`
  - `text`
  - `number-slider`
  - `color`
  - `multi-select`
- Use tweak values to change visible page styling/content.
- Cleanup must remove all injected nodes/styles.

Acceptance criteria:

- Build stamps ULID if needed.
- Registry includes typed tweaks.

Verify:

- `corepack pnpm -F @platform/extension build`

## Task 2: Add Shadow DOM Panel Experiment

Files:

- `experiments/andrew/shadow-panel/manifest.json`
- `experiments/andrew/shadow-panel/experiment.ts`
- `apps/extension/src/content/shadow-dom.test.ts`

Action:

- Use WXT `createShadowRootUi` or the closest supported WXT utility in content-script context.
- Mount a small panel inside a ShadowRoot.
- Return cleanup that unmounts and removes the host.
- Avoid global style injection.

Acceptance criteria:

- Experiment builds as a separate chunk.
- Test or build-time source assertion confirms ShadowRoot usage.

Verify:

- `corepack pnpm vitest run apps/extension/src/content/shadow-dom.test.ts`
- `corepack pnpm -F @platform/extension build`

## Task 3: Extend Build Tests For Reference Experiments

Files:

- `tools/build-experiments.test.ts`

Action:

- Add coverage that registry preserves non-empty typed tweaks.
- Add coverage for multiple experiment chunks.

Acceptance criteria:

- Both reference experiments appear in build output.

Verify:

- `corepack pnpm vitest run tools/build-experiments.test.ts`

</tasks>

<verification>

- `corepack pnpm test -- --run`
- `corepack pnpm typecheck`
- `corepack pnpm lint`
- `corepack pnpm build`
</verification>

<success_criteria>

- A real experiment demonstrates 3+ tweak types end-to-end.
- A real experiment demonstrates Shadow DOM mounting without host-page global CSS.
</success_criteria>
