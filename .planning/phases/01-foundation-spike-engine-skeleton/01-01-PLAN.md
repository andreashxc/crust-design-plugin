---
phase: 01-foundation-spike-engine-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - tsconfig.json
  - .gitignore
  - .nvmrc
  - biome.json
  - lefthook.yml
  - .github/workflows/ci.yml
  - vitest.workspace.ts
  - tests/fixtures/manifests/valid.json
  - tests/fixtures/manifests/missing-id.json
  - tests/fixtures/manifests/bad-world.json
  - tests/fixtures/manifests/empty-match.json
  - tests/fixtures/manifests/over-280-description.json
  - tests/fixtures/manifests/lowercase-ulid.json
  - tests/sanity.test.ts
autonomous: true
requirements:
  - infra
must_haves:
  truths:
    - "`pnpm install --frozen-lockfile` succeeds at repo root with no resolution errors"
    - "`pnpm test --run` exits 0 (at least one passing sanity test)"
    - "`pnpm typecheck` exits 0 against `tsconfig.json` project references"
    - "`pnpm lint` (Biome) exits 0 on the empty repo"
    - "Pre-commit hook blocks commits whose `pnpm lint` / `pnpm typecheck` / `pnpm test --run` / `pnpm check-csp` fail (check-csp script will exist by Plan 05; the hook entry can be present and the script wired up later)"
    - "GitHub Actions CI workflow file is valid YAML and runs the same gates on push + PR"
  artifacts:
    - path: "package.json"
      provides: "root pnpm workspace, scripts, engines, packageManager"
      contains: "\"private\": true"
    - path: "pnpm-workspace.yaml"
      provides: "monorepo workspace declaration"
      contains: "apps/*"
    - path: "tsconfig.base.json"
      provides: "shared strict TS config"
      contains: "\"strict\": true"
    - path: "biome.json"
      provides: "lint + format config"
    - path: "lefthook.yml"
      provides: "pre-commit hook running gates"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline"
    - path: "vitest.workspace.ts"
      provides: "Vitest workspace aggregator"
    - path: "tests/fixtures/manifests/valid.json"
      provides: "shared manifest fixture used by SDK + build-plugin tests"
    - path: "tests/sanity.test.ts"
      provides: "first passing test that proves Vitest wiring"
  key_links:
    - from: "lefthook.yml"
      to: "package.json scripts"
      via: "pnpm lint / typecheck / test --run / check-csp"
      pattern: "pnpm (lint|typecheck|test|check-csp)"
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "same script names as lefthook"
      pattern: "pnpm (lint|typecheck|test|check-csp|build)"
---

<objective>
Make the empty repo a working pnpm + TypeScript + Biome + Vitest + lefthook + GitHub Actions monorepo. ZERO extension code. ZERO experiments. After this plan, downstream plans can `pnpm install` and `pnpm test --run`. This is the test infrastructure (Wave 0) the rest of Phase 1 builds on.

Purpose: every Phase-1 success criterion (and every later phase) depends on running tests, lint, typecheck, and the build pipeline. Those tools cannot run on a directory that contains only `.planning/` and `.git/`. This plan unblocks all subsequent waves.

Output: a green `pnpm install`, a green Vitest sanity test, a green Biome lint, a configured pre-commit hook (script targets exist or are stub-routed where Plan 05 will fill them), and a CI workflow ready to receive subsequent commits.
</objective>

<execution_context>
@/Users/andreas/Library/Mobile Documents/com~apple~CloudDocs/Code/gsd-test/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreas/Library/Mobile Documents/com~apple~CloudDocs/Code/gsd-test/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-spike-engine-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-spike-engine-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-spike-engine-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-spike-engine-skeleton/01-VALIDATION.md

<interfaces>
<!-- This plan creates the contracts (configs, scripts) that all later plans consume. -->
<!-- Verbatim references from RESEARCH.md / PATTERNS.md are inlined into each task action. -->
<!-- pnpm version pin (RESEARCH "Pinned Versions" + PATTERNS SP-6): pnpm@10.33.2; node >=24.0.0 -->
<!-- Biome pin: @biomejs/biome ^2.4.13 -->
<!-- Vitest pin: vitest ^4.1.5; @vitest/ui (no version constraint — let pnpm pick latest matching) -->
<!-- lefthook pin: lefthook ^2.1.6 -->
<!-- TypeScript pin (RESEARCH R5 — supersedes CONTEXT D-02 "5.6+"): typescript ^6.0.3 -->

<!-- Root package.json scripts (called by lefthook + CI; defined ONCE here): -->
<!--   "dev": "pnpm -F @platform/extension dev"  (apps/extension defines this in Plan 03) -->
<!--   "build": "pnpm -r build"                    (each workspace exposes its own build) -->
<!--   "lint": "biome check ."                     (Biome lint on whole repo) -->
<!--   "typecheck": "tsc -b --pretty"              (TS project references; tsconfig.json refs Plan 02 + Plan 03) -->
<!--   "test": "vitest"                            (Vitest workspace mode; --run added by callers) -->
<!--   "check-csp": "tsx scripts/check-csp.ts"     (script created in Plan 05) -->

<!-- tsconfig.base.json (RESEARCH Step 1.3 verbatim settings): -->
<!--   strict: true, noUncheckedIndexedAccess: true, target: ES2022, moduleResolution: bundler, -->
<!--   esModuleInterop: true, skipLibCheck: true, forceConsistentCasingInFileNames: true, -->
<!--   isolatedModules: true, declaration: false (apps/SDK turn it on as needed) -->

<!-- Shared manifest fixtures live under tests/fixtures/manifests/ — ONE copy used by both -->
<!-- packages/experiment-sdk/src/manifest.test.ts (Plan 02) AND tools/build-experiments.test.ts (Plan 04). -->
<!-- This avoids duplication and lets fixture-only changes propagate to both test suites. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root scaffolding (package.json, pnpm-workspace.yaml, tsconfig.base.json, tsconfig.json, .gitignore, .nvmrc)</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, tsconfig.json, .gitignore, .nvmrc</files>
  <read_first>
    - .planning/phases/01-foundation-spike-engine-skeleton/01-RESEARCH.md (Step 1, Step 1.1-1.4, Pinned Versions table, Risks R5)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-PATTERNS.md (Group A — Repo Scaffolding, SP-6 pinned versions)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-CONTEXT.md (D-08 pnpm workspaces, D-09 monorepo layout, D-02 TypeScript)
  </read_first>
  <action>
    Create the root scaffolding for a pnpm + TypeScript monorepo. Use the EXACT pins below (per RESEARCH Pinned Versions + Risk R5; do NOT regress to CONTEXT's "TS 5.6+" / "Zod 3.23+" — those are stale).

    **`package.json`** (repo root):
    ```json
    {
      "name": "designer-experiment-platform",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "packageManager": "pnpm@10.33.2",
      "engines": { "node": ">=24.0.0" },
      "scripts": {
        "dev": "pnpm -F @platform/extension dev",
        "build": "pnpm -r build",
        "lint": "biome check .",
        "lint:fix": "biome check --write .",
        "typecheck": "tsc -b --pretty",
        "test": "vitest",
        "check-csp": "tsx scripts/check-csp.ts"
      },
      "devDependencies": {
        "@biomejs/biome": "^2.4.13",
        "lefthook": "^2.1.6",
        "tsx": "^4.20.0",
        "typescript": "^6.0.3",
        "vitest": "^4.1.5",
        "@vitest/ui": "^4.1.5"
      }
    }
    ```
    Notes: `tsx` is needed in Plan 05 to run `scripts/check-csp.ts`; install it now so CI doesn't bounce. `@vitest/ui` is dev-only convenience. The `build` / `dev` scripts intentionally route to workspace targets that will be created in Plans 02-03 — `pnpm -r build` is a no-op until packages declare `build` scripts.

    **`pnpm-workspace.yaml`** (RESEARCH Step 1.2 verbatim):
    ```yaml
    packages:
      - apps/*
      - packages/*
    ```

    **`tsconfig.base.json`** (RESEARCH Step 1.3 verbatim settings + standard hygiene):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "lib": ["ES2022", "DOM", "DOM.Iterable"],
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "isolatedModules": true,
        "resolveJsonModule": true,
        "verbatimModuleSyntax": true,
        "useDefineForClassFields": true,
        "noEmit": true,
        "jsx": "react-jsx"
      },
      "exclude": ["**/node_modules", "**/dist", "**/.output", "**/.wxt"]
    }
    ```

    **`tsconfig.json`** (root project-references stub; will reference SDK and extension once they exist in Plans 02-03 — leave references EMPTY for now so Plan 02 can edit it without conflict):
    ```json
    {
      "files": [],
      "references": []
    }
    ```
    Plan 02 adds `{ "path": "./packages/experiment-sdk" }`; Plan 03 adds `{ "path": "./apps/extension" }`. Both later plans MUST patch this file (record this as a known shared file in their plans — handled by `files_modified` ordering).

    **`.gitignore`** (RESEARCH Step 1.4 verbatim, plus standard Node):
    ```
    node_modules/
    apps/extension/.output/
    apps/extension/.wxt/
    dist/
    coverage/
    *.log
    .DS_Store
    .env
    .env.local
    ```

    **`.nvmrc`** (single line):
    ```
    24
    ```

    Do NOT run `pnpm install` from this task — that happens in Task 5's verification (after lefthook config is present so postinstall doesn't fail).
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f tsconfig.base.json && test -f tsconfig.json && test -f .gitignore && grep -q '"packageManager": "pnpm@10.33.2"' package.json && grep -q '"typescript": "\^6.0.3"' package.json && grep -q '"strict": true' tsconfig.base.json && grep -q '"noUncheckedIndexedAccess": true' tsconfig.base.json && grep -q '^node_modules/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` exists at repo root with `"private": true`, `"packageManager": "pnpm@10.33.2"`, `"engines": { "node": ">=24.0.0" }`.
    - `package.json` `scripts` contains all of: `dev`, `build`, `lint`, `lint:fix`, `typecheck`, `test`, `check-csp` (exact names; `lefthook.yml` and `.github/workflows/ci.yml` invoke these).
    - `package.json` `devDependencies` contains EXACTLY these names with the listed pins: `@biomejs/biome ^2.4.13`, `lefthook ^2.1.6`, `tsx ^4.20.0`, `typescript ^6.0.3`, `vitest ^4.1.5`, `@vitest/ui ^4.1.5`.
    - `pnpm-workspace.yaml` lists exactly `apps/*` and `packages/*`.
    - `tsconfig.base.json` contains `"strict": true`, `"noUncheckedIndexedAccess": true`, `"target": "ES2022"`, `"moduleResolution": "bundler"`, `"verbatimModuleSyntax": true`, `"isolatedModules": true`.
    - `tsconfig.json` (root) has `"files": []` and `"references": []` (intentionally empty — extended by Plans 02 + 03).
    - `.gitignore` contains lines starting with `node_modules/`, `apps/extension/.output/`, `apps/extension/.wxt/`, `dist/`, `*.log`.
    - `.nvmrc` contains the single character/string `24`.
  </acceptance_criteria>
  <done>Root config files exist; nothing installed yet; no scripts run yet (that is Task 5).</done>
</task>

<task type="auto">
  <name>Task 2: Biome config (biome.json)</name>
  <files>biome.json</files>
  <read_first>
    - .planning/phases/01-foundation-spike-engine-skeleton/01-RESEARCH.md (Step 1.5, External APIs / Pinned Versions for `@biomejs/biome ^2.4.13`)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-PATTERNS.md (Group A — biome.json convention)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-CONTEXT.md (D-06 Biome single binary)
  </read_first>
  <action>
    Write `biome.json` at repo root with the configuration below. Do NOT run `biome init` — write the file directly so the executor controls every key.

    ```json
    {
      "$schema": "https://biomejs.dev/schemas/2.4.13/schema.json",
      "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
      "files": {
        "ignore": [
          "**/node_modules/**",
          "**/dist/**",
          "**/.output/**",
          "**/.wxt/**",
          "**/coverage/**",
          "**/.next/**"
        ]
      },
      "formatter": {
        "enabled": true,
        "indentStyle": "space",
        "indentWidth": 2,
        "lineWidth": 100,
        "lineEnding": "lf"
      },
      "linter": {
        "enabled": true,
        "rules": {
          "recommended": true,
          "suspicious": {
            "noExplicitAny": "error"
          },
          "style": {
            "useImportType": "error"
          }
        }
      },
      "javascript": {
        "formatter": {
          "quoteStyle": "single",
          "semicolons": "always",
          "trailingCommas": "all"
        }
      },
      "json": {
        "formatter": {
          "trailingCommas": "none"
        }
      }
    }
    ```

    Why these settings (per RESEARCH Step 1.5):
    - `formatter.indentStyle: "space"` — matches RESEARCH override.
    - `linter.rules.suspicious.noExplicitAny: "error"` — matches RESEARCH override.
    - `vcs.useIgnoreFile: true` — Biome respects `.gitignore` (no double-listing).
    - `style.useImportType: "error"` — pairs with `verbatimModuleSyntax: true` in tsconfig (mutually reinforcing; prevents type-only imports leaking into runtime bundles).

    Do NOT add custom CSP rules here — the CSP regex check is a separate script (`scripts/check-csp.ts`, created in Plan 05) per CONTEXT D-23.
  </action>
  <verify>
    <automated>test -f biome.json && grep -q '"noExplicitAny": "error"' biome.json && grep -q '"indentStyle": "space"' biome.json && grep -q '"useIgnoreFile": true' biome.json</automated>
  </verify>
  <acceptance_criteria>
    - File `biome.json` exists at repo root.
    - Contains `"$schema": "https://biomejs.dev/schemas/2.4.13/schema.json"` (matches the pinned `@biomejs/biome ^2.4.13`).
    - Contains `"formatter": { ..., "indentStyle": "space", ... }`.
    - Contains `"linter.rules.suspicious.noExplicitAny": "error"` (or equivalent nested JSON).
    - Contains `"vcs.useIgnoreFile": true`.
    - `files.ignore` array includes `**/node_modules/**`, `**/dist/**`, `**/.output/**`, `**/.wxt/**`.
  </acceptance_criteria>
  <done>biome.json present and valid JSON; will be exercised by `pnpm lint` after install.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest workspace + sanity test + manifest fixtures</name>
  <files>vitest.workspace.ts, tests/sanity.test.ts, tests/fixtures/manifests/valid.json, tests/fixtures/manifests/missing-id.json, tests/fixtures/manifests/bad-world.json, tests/fixtures/manifests/empty-match.json, tests/fixtures/manifests/over-280-description.json, tests/fixtures/manifests/lowercase-ulid.json</files>
  <read_first>
    - .planning/phases/01-foundation-spike-engine-skeleton/01-RESEARCH.md (Validation Architecture, Wave 0 Gaps, "Test fixtures" bullet)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-PATTERNS.md (Group D — Vitest Wiring, manifest test cases)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-VALIDATION.md (Wave 0 Requirements list)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-CONTEXT.md (D-16 Zod schema — fixtures must hit each validation rule)
  </read_first>
  <action>
    Create the Vitest workspace aggregator, one passing sanity test, and the SHARED manifest fixtures used by Plan 02 (`packages/experiment-sdk/src/manifest.test.ts`) and Plan 04 (`tools/build-experiments.test.ts`).

    **`vitest.workspace.ts`** (root):
    ```ts
    import { defineWorkspace } from 'vitest/config';

    export default defineWorkspace([
      './apps/*/vitest.config.ts',
      './packages/*/vitest.config.ts',
      // Root-level test patterns (sanity, scripts, tools tests):
      {
        test: {
          name: 'root',
          include: [
            'tests/**/*.test.ts',
            'scripts/**/*.test.ts',
            'tools/**/*.test.ts',
          ],
          environment: 'node',
        },
      },
    ]);
    ```
    Note: the glob `./apps/*/vitest.config.ts` will resolve to nothing until Plan 03 creates it; Vitest tolerates non-matching globs and will not error.

    **`tests/sanity.test.ts`**:
    ```ts
    import { describe, expect, it } from 'vitest';

    describe('phase-1 sanity', () => {
      it('vitest runs', () => {
        expect(1 + 1).toBe(2);
      });
    });
    ```
    This test exists ONLY to prove the toolchain is wired. Plan 02 replaces the role of "primary tests" with the SDK manifest tests.

    **Manifest fixtures** — six files under `tests/fixtures/manifests/`. These are CONSUMED by Plan 02 and Plan 04 tests. Each fixture is a complete JSON manifest with one specific deviation from the valid baseline (per CONTEXT D-16 Zod rules and RESEARCH "Wave 0 Gaps"):

    `tests/fixtures/manifests/valid.json`:
    ```json
    {
      "id": "01J0ABCDEFGHJKMNPQRSTVWXYZ",
      "name": "Smoke pink",
      "author": "andrew",
      "description": "Turns ya.ru pink to verify foundation",
      "scope": { "match": ["*://*.ya.ru/*", "*://ya.ru/*"] },
      "world": "isolated",
      "tweaks": []
    }
    ```

    `tests/fixtures/manifests/missing-id.json` — same as valid.json but with `"id": ""` (empty string — represents a manifest before the build plugin runs ULID generation; Plan 04 tests use it to verify ULID write-back).

    `tests/fixtures/manifests/bad-world.json` — same as valid.json but with `"world": "iso"` (Zod enum should reject; expected error: `world: ...`).

    `tests/fixtures/manifests/empty-match.json` — same as valid.json but with `"scope": { "match": [] }` (Zod `.min(1)` should reject).

    `tests/fixtures/manifests/over-280-description.json` — same as valid.json but `description` is 281 characters (exactly one over the `.max(280)` limit). Use a string of 281 letter "a"s to make the test deterministic.

    `tests/fixtures/manifests/lowercase-ulid.json` — same as valid.json but `"id": "01j0abcdefghjkmnpqrstvwxyz"` (lowercase — Crockford base32 uppercase regex should reject).

    All fixtures use 2-space indentation and end with a trailing newline (matches `JSON.stringify(value, null, 2) + '\n'` so Plan 04's idempotency tests don't false-fail).
  </action>
  <verify>
    <automated>test -f vitest.workspace.ts && test -f tests/sanity.test.ts && test -f tests/fixtures/manifests/valid.json && test -f tests/fixtures/manifests/missing-id.json && test -f tests/fixtures/manifests/bad-world.json && test -f tests/fixtures/manifests/empty-match.json && test -f tests/fixtures/manifests/over-280-description.json && test -f tests/fixtures/manifests/lowercase-ulid.json && grep -q '"id": "01J0ABCDEFGHJKMNPQRSTVWXYZ"' tests/fixtures/manifests/valid.json && grep -q '"world": "iso"' tests/fixtures/manifests/bad-world.json && python3 -c "import json; assert len(json.load(open('tests/fixtures/manifests/over-280-description.json'))['description']) == 281"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.workspace.ts` exists, exports default from `defineWorkspace([...])`, includes `'./apps/*/vitest.config.ts'`, `'./packages/*/vitest.config.ts'`, and a root entry covering `tests/**/*.test.ts` + `scripts/**/*.test.ts` + `tools/**/*.test.ts`.
    - `tests/sanity.test.ts` exists; contains `expect(1 + 1).toBe(2)`.
    - All six fixture files exist under `tests/fixtures/manifests/`.
    - `valid.json` parses as JSON and has `id` matching `^[0-9A-HJKMNP-TV-Z]{26}$`.
    - `bad-world.json` has `"world": "iso"` (string mismatch — Zod will reject).
    - `empty-match.json` has `"scope": { "match": [] }`.
    - `over-280-description.json` has exactly 281 characters in `description` (verified by `python3` JSON load + len check in the verify command).
    - `lowercase-ulid.json` has `id` containing lowercase letters.
    - `missing-id.json` has `"id": ""`.
  </acceptance_criteria>
  <done>Vitest workspace defined; sanity test in place; fixtures ready for Plan 02 + Plan 04 consumption.</done>
</task>

<task type="auto">
  <name>Task 4: GitHub Actions CI workflow (.github/workflows/ci.yml)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation-spike-engine-skeleton/01-RESEARCH.md (Step 1.7, External APIs)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-PATTERNS.md (Group A — `.github/workflows/ci.yml`)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-CONTEXT.md (D-24 — pre-commit + GitHub Actions both run lint/typecheck/test)
    - package.json (script names this workflow invokes — written in Task 1)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` exactly as below. Use `pnpm/action-setup@v4` (current stable per RESEARCH External APIs) and `actions/setup-node@v4`.

    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    permissions:
      contents: read

    jobs:
      check:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Setup pnpm
            uses: pnpm/action-setup@v4
            with:
              version: 10.33.2
              run_install: false

          - name: Setup Node
            uses: actions/setup-node@v4
            with:
              node-version: 24
              cache: pnpm

          - name: Install dependencies
            run: pnpm install --frozen-lockfile

          - name: Lint
            run: pnpm lint

          - name: Typecheck
            run: pnpm typecheck

          - name: Test
            run: pnpm test --run

          - name: Check CSP
            run: pnpm check-csp

          - name: Build
            run: pnpm build
    ```

    Notes:
    - The workflow runs every gate the pre-commit runs PLUS `pnpm build` (which is `pnpm -r build` — no-op until Plan 03 / Plan 04).
    - `pnpm check-csp` calls `tsx scripts/check-csp.ts`. The script does NOT exist yet — Plan 05 creates it. Until Plan 05 ships, the workflow will fail at the "Check CSP" step. That is acceptable: CI is not expected to be green until Phase 1 finishes, and downstream plans will commit the missing pieces. Document this in the plan summary.
    - `--frozen-lockfile` requires `pnpm-lock.yaml` to be committed; Task 5 generates it.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'pnpm/action-setup@v4' .github/workflows/ci.yml && grep -q 'version: 10.33.2' .github/workflows/ci.yml && grep -q 'node-version: 24' .github/workflows/ci.yml && grep -q 'pnpm lint' .github/workflows/ci.yml && grep -q 'pnpm typecheck' .github/workflows/ci.yml && grep -q 'pnpm test --run' .github/workflows/ci.yml && grep -q 'pnpm check-csp' .github/workflows/ci.yml && grep -q 'pnpm build' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists.
    - Contains `name: CI`.
    - Triggers on `push` to `main` and on `pull_request` targeting `main`.
    - Uses `pnpm/action-setup@v4` with `version: 10.33.2`.
    - Uses `actions/setup-node@v4` with `node-version: 24` and `cache: pnpm`.
    - Steps execute (in order): `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test --run`, `pnpm check-csp`, `pnpm build`.
  </acceptance_criteria>
  <done>CI workflow committed; will go green once Plans 02–05 ship their pieces (lockfile, packages, csp script).</done>
</task>

<task type="auto">
  <name>Task 5: lefthook + first install (lefthook.yml + pnpm install)</name>
  <files>lefthook.yml, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/01-foundation-spike-engine-skeleton/01-RESEARCH.md (Step 1.6 — pre-commit commands; Risks R4 — lefthook chosen)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-PATTERNS.md (Group A — lefthook.yml)
    - .planning/phases/01-foundation-spike-engine-skeleton/01-CONTEXT.md (D-23 + D-24 — pre-commit + GH Actions both run lint/typecheck/test/check-csp)
    - package.json (script names — written in Task 1)
  </read_first>
  <action>
    Step 1 — write `lefthook.yml` at repo root:
    ```yaml
    pre-commit:
      parallel: true
      commands:
        lint:
          run: pnpm lint
        typecheck:
          run: pnpm typecheck
        test:
          run: pnpm test --run
        check-csp:
          run: pnpm check-csp
    ```

    Step 2 — run install + hook setup. Run these EXACT commands from the repo root, in order:
    ```bash
    pnpm install
    pnpm exec lefthook install
    ```
    `pnpm install` will:
    - Resolve all root devDependencies (Biome, lefthook, tsx, typescript, vitest, @vitest/ui).
    - Generate `pnpm-lock.yaml` (this file MUST be committed — CI uses `--frozen-lockfile`).
    - Create `node_modules/` (already in `.gitignore`).

    `pnpm exec lefthook install` writes Git's `.git/hooks/pre-commit` shim that invokes `lefthook run pre-commit`.

    Step 3 — verify the toolchain runs:
    ```bash
    pnpm lint        # MUST exit 0 (Biome on a near-empty repo finds nothing to complain about)
    pnpm typecheck   # MUST exit 0 (no .ts files yet outside node_modules → tsc -b is a no-op)
    pnpm test --run  # MUST exit 0 (sanity test passes)
    ```
    Do NOT run `pnpm check-csp` yet — `scripts/check-csp.ts` does not exist (Plan 05). `pnpm build` is also a no-op until Plan 03.

    Known caveat: `pnpm typecheck` runs `tsc -b --pretty` against `tsconfig.json` (Task 1) which has `"references": []`. With no references, `tsc -b` is effectively a no-op and exits 0. This is intentional — Plans 02 and 03 add references.

    Commit `pnpm-lock.yaml`. Do NOT commit `node_modules/`.
  </action>
  <verify>
    <automated>test -f lefthook.yml && test -f pnpm-lock.yaml && test -f .git/hooks/pre-commit && pnpm lint && pnpm typecheck && pnpm test --run && grep -q 'pnpm lint' lefthook.yml && grep -q 'pnpm typecheck' lefthook.yml && grep -q 'pnpm test --run' lefthook.yml && grep -q 'pnpm check-csp' lefthook.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `lefthook.yml` exists; declares a `pre-commit` block with four commands: `lint` → `pnpm lint`, `typecheck` → `pnpm typecheck`, `test` → `pnpm test --run`, `check-csp` → `pnpm check-csp`.
    - File `pnpm-lock.yaml` exists at repo root and is non-empty (contains the resolved tree for the six root devDependencies).
    - File `.git/hooks/pre-commit` exists (lefthook installed). The file contents reference `lefthook run pre-commit`.
    - `pnpm lint` exits 0.
    - `pnpm typecheck` exits 0 (no files to check yet — empty references is a no-op).
    - `pnpm test --run` exits 0 with the sanity test passing.
    - `node_modules/` exists at repo root but is NOT committed (verified by `.gitignore` from Task 1).
  </acceptance_criteria>
  <done>Toolchain installed, lockfile committed, hooks active, three of the five quality gates verified green. `pnpm check-csp` and `pnpm build` are intentionally not yet runnable — they wire up in Plans 03–05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → repo | A team member with commit access can introduce arbitrary code; pre-commit hook is the first checkpoint |
| CI runner ↔ repo | GitHub Actions executes the full gate set on every push/PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering / Elevation | developer commit | mitigate | `lefthook.yml` runs `pnpm check-csp` pre-commit (script lands in Plan 05). CI re-runs the same check. Defense-in-depth — any single bypass is caught by the other layer. |
| T-1-PM | Tampering | dependency tree | mitigate | `packageManager: pnpm@10.33.2` pin + `pnpm install --frozen-lockfile` in CI. Prevents silent upgrade of compromised deps between developer and CI. |
| T-1-IS | Information Disclosure | secrets in repo | accept (mitigate via `.gitignore`) | `.env`, `.env.local` listed in `.gitignore`. Closed team, threat is incidental commit of designer LLM keys (Phase 4 risk). Phase 1 has no secrets. |

This plan does NOT mitigate T-1-01 / T-1-02 / T-1-03 / T-1-04 / T-1-06 — those are addressed by Plans 02–04 (manifest schema, stateless SW, world routing, error isolation, author-vs-folder check).
</threat_model>

<verification>
After this plan completes, the following commands MUST all exit 0 from repo root:

1. `pnpm install --frozen-lockfile` — lockfile parses, all 6 root devDependencies resolve.
2. `pnpm lint` — Biome reports zero errors (empty repo).
3. `pnpm typecheck` — `tsc -b` no-op exits 0 (empty project references).
4. `pnpm test --run` — Vitest finds and passes `tests/sanity.test.ts`.
5. `git diff --stat` shows committed files; `node_modules/` is absent from `git ls-files`.
6. `cat .git/hooks/pre-commit` shows lefthook shim text.

`pnpm check-csp` and `pnpm build` are intentionally NOT yet expected to run — Plans 03–05 add the missing pieces. Document this in the plan SUMMARY so executors of subsequent plans don't blame Plan 01.
</verification>

<success_criteria>
- Repo is a valid pnpm 10 monorepo (root + workspace declaration + lockfile committed).
- Biome lint, TypeScript typecheck, Vitest test all run on a fresh `pnpm install`.
- lefthook pre-commit hook is installed locally and references all four gate commands.
- GitHub Actions CI workflow exists and is structurally valid YAML.
- Six manifest fixtures exist under `tests/fixtures/manifests/` for Plan 02 + Plan 04 consumption.
- ZERO extension code, ZERO experiments, ZERO build plugin yet — those are Plans 02–05.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-spike-engine-skeleton/01-01-SUMMARY.md` documenting:
1. Files created (full list).
2. Verified-running commands (`pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm test --run`).
3. Known not-yet-runnable commands (`pnpm check-csp`, `pnpm build`) and which plan ships each.
4. Any deviations from the action text (e.g. if `tsx ^4.20.0` actually resolved to a different minor — record actual lockfile pin).
5. Threat IDs mitigated (T-1-05, T-1-PM).
</output>
