---
phase: 06-distribution-acceptance
plan: 06-01
type: implementation
wave: 0
depends_on: []
requirements: [BLD-06, DIST-01]
files_modified:
  - package.json
  - apps/extension/package.json
  - .github/workflows/release.yml
  - docs/ONBOARDING.md
autonomous: true
---

<objective>
Add the root packaging command surface and GitHub Releases workflow for sideload artifacts.
</objective>

<tasks>

## Task 1: Add Root Packaging Scripts

Read first:

- `package.json`
- `apps/extension/package.json`

Action:

- Add root scripts for packaging the Chrome extension via WXT.
- Preserve existing `corepack pnpm build`.
- Add a command that produces WXT zip artifacts.
- If no true `.crx` signing key exists, name the command/artifact honestly and document the signing limitation.

Acceptance criteria:

- `corepack pnpm package:chrome` or equivalent runs from repo root.
- The command produces a zip or packaged artifact under `apps/extension/.output`.

## Task 2: Add Release Workflow

Read first:

- `.github/workflows/ci.yml`

Action:

- Add `.github/workflows/release.yml`.
- Trigger on tags `v*`.
- Reuse Node 24 + pnpm 10.33.2 + frozen lockfile.
- Run lint/typecheck/test/CSP/build/package.
- Upload unpacked/zip artifacts to GitHub Releases.

Acceptance criteria:

- Workflow uses `contents: write` only for release upload.
- Workflow does not run on every branch push.

## Task 3: Document Release Install Path

Read first:

- `docs/ONBOARDING.md`

Action:

- Add a short section explaining how to install from a release artifact.
- Keep dev onboarding unchanged.

</tasks>

<verification>

- `corepack pnpm package:chrome`
- `corepack pnpm typecheck`
- `corepack pnpm check-csp`

</verification>
