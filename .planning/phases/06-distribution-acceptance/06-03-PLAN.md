---
phase: 06-distribution-acceptance
plan: 06-03
type: implementation
wave: 2
depends_on: [06-02]
requirements: [ACC-01, ACC-02, ACC-03, ACC-04, ACC-06]
files_modified:
  - experiments/designer1/*
  - experiments/designer2/*
  - experiments/designer3/*
  - docs/EXPERIMENT_AUTHORING.md
  - tools/build-experiments.test.ts
autonomous: true
---

<objective>
Add small acceptance fixture experiments for designer1, designer2, and designer3 that cover the v1 acceptance matrix.
</objective>

<tasks>

## Task 1: Create Three Designer Folders

Action:

- Add `experiments/designer1/*`, `experiments/designer2/*`, and `experiments/designer3/*`.
- Each designer must have at least one experiment.
- Keep all scopes on ya.ru unless a specific helper smoke needs a narrower URL.

Acceptance criteria:

- Build registry includes all three authors.

## Task 2: Cover Required Behaviors

Action:

- At least one fixture uses `helpers.llm`.
- At least one fixture uses `helpers.fetchPage`.
- At least one fixture has 3+ tweak types and reuses established tweak patterns.
- At least two fixtures can be composed on the same ya.ru tab without duplicate DOM IDs.

Acceptance criteria:

- Code search confirms `helpers.llm` and `helpers.fetchPage`.
- Manifest scan confirms one fixture with 3+ tweak definitions.

## Task 3: Validate Fixtures

Action:

- Add or extend build-experiment tests to assert acceptance fixture presence and metadata.

</tasks>

<verification>

- `corepack pnpm vitest run tools/build-experiments.test.ts`
- `corepack pnpm typecheck`
- `corepack pnpm build`

</verification>
