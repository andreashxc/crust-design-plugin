---
phase: 06-distribution-acceptance
plan: 06-02
type: implementation
wave: 1
depends_on: [06-01]
requirements: [SHR-03, ACC-05]
files_modified:
  - scripts/fork-experiment.ts
  - package.json
  - apps/extension/entrypoints/popup/components/ExperimentRow.tsx
  - apps/extension/entrypoints/popup/App.test.tsx
  - docs/EXPERIMENT_AUTHORING.md
autonomous: true
---

<objective>
Ship the v1 fork-to-my-folder flow through a truthful popup copy command and a repo-mutating CLI script.
</objective>

<tasks>

## Task 1: Add Fork CLI

Read first:

- `scripts/create-experiment.ts`
- `scripts/save-preset.ts`
- `tools/build-experiments.ts`

Action:

- Add `scripts/fork-experiment.ts`.
- Accept `<source-id-or-author/folder> <target-author> [target-folder]`.
- Copy `manifest.json`, `experiment.ts`, `description.md`, and `presets/`.
- Update `manifest.author` to target author.
- Reset `manifest.id` to empty so the next build stamps a new ULID.
- Refuse to overwrite an existing target folder.
- Add root script `fork-experiment`.

Acceptance criteria:

- CLI smoke forks a fixture into a temp root and the target manifest has `id: ""`.

## Task 2: Add Popup Fork Command

Read first:

- `apps/extension/entrypoints/popup/components/ExperimentRow.tsx`
- `apps/extension/entrypoints/popup/App.test.tsx`

Action:

- Add compact "Copy fork command" action.
- The command must include `corepack pnpm fork-experiment`.
- Default target author can be user-entered in a small input or use a copyable placeholder; do not block existing row ergonomics.
- UI must say command copied, not forked.

Acceptance criteria:

- Component test asserts clipboard text contains source author/folder and `corepack pnpm fork-experiment`.

## Task 3: Docs

- Document fork command in `docs/EXPERIMENT_AUTHORING.md`.

</tasks>

<verification>

- `corepack pnpm vitest run apps/extension/entrypoints/popup/App.test.tsx`
- `corepack pnpm typecheck`
- `corepack pnpm check-csp`

</verification>
