---
phase: 04-helpers-llm-integration
plan: 04-02
type: implementation
wave: 1
depends_on: [04-01]
requirements: [HLP-01, HLP-03, HLP-05]
files_modified:
  - apps/extension/entrypoints/options/index.html
  - apps/extension/entrypoints/options/main.tsx
  - apps/extension/entrypoints/options/App.tsx
  - apps/extension/entrypoints/options/App.test.tsx
  - apps/extension/src/options/store.ts
  - apps/extension/src/styles/options.css
  - apps/extension/entrypoints/popup/App.tsx
  - apps/extension/src/popup/store.ts
autonomous: true
---

<objective>
Create the Curst options page for provider keys, defaults, cost guard settings, and diagnostics. This plan ships the UI and storage integration but uses stubbed provider tests until provider handlers land in 04-03.
</objective>

<tasks>

## Task 1: Add WXT Options Entrypoint

Files:

- `apps/extension/entrypoints/options/index.html`
- `apps/extension/entrypoints/options/main.tsx`
- `apps/extension/entrypoints/options/App.tsx`
- `apps/extension/src/styles/options.css`

Action:

- Add `entrypoints/options/index.html` with title `Curst Options` and `manifest.open_in_tab=true`.
- Bootstrap React like the popup.
- Reuse compact dark styling and shadcn primitives.

Acceptance criteria:

- `corepack pnpm -F @platform/extension build` emits `options.html`.
- Options page does not import popup-only code except shared UI primitives.

## Task 2: Providers UI

Files:

- `apps/extension/entrypoints/options/App.tsx`
- `apps/extension/src/options/store.ts`
- `apps/extension/entrypoints/options/App.test.tsx`

Action:

- Render OpenAI and Anthropic provider rows.
- Each row has masked key input, show/hide control, Save, Clear, Test, and status.
- Save/Clear writes through storage helpers from 04-01.
- Test button can call a placeholder typed message returning "not wired yet" until 04-03, but the UI state must be complete.

Acceptance criteria:

- Saved keys are masked on reload.
- Show/hide affects only local input display.
- Tests cover save, clear, mask toggle, and status rendering.

## Task 3: Defaults, Cost Guard, Diagnostics UI

Files:

- `apps/extension/entrypoints/options/App.tsx`
- `apps/extension/src/options/store.ts`
- `apps/extension/entrypoints/options/App.test.tsx`

Action:

- Add default provider select.
- Add OpenAI/Anthropic model id text inputs.
- Add max output tokens, cache TTL, max attempts, warning threshold, and apply rate-limit inputs.
- Add diagnostics with session calls/tokens, last LLM error, clear cache, reset session.

Acceptance criteria:

- Invalid numeric values do not persist.
- Default provider with missing key shows inline warning in options.
- Clear cache and reset session call storage helpers.

## Task 4: Popup Header Counter Plumbing

Files:

- `apps/extension/entrypoints/popup/App.tsx`
- `apps/extension/src/popup/store.ts`
- `apps/extension/entrypoints/popup/App.test.tsx`

Action:

- Extend popup store to load `llm:session`.
- Add compact calls/tokens indicator in the header when session stats exist.
- Do not add global missing-key banners.

Acceptance criteria:

- Existing popup tests pass.
- New test covers visible session calls/tokens.

</tasks>

<verification>

- `corepack pnpm vitest run apps/extension/entrypoints/options/App.test.tsx apps/extension/entrypoints/popup/App.test.tsx`
- `corepack pnpm typecheck`
- `corepack pnpm -F @platform/extension build`

</verification>

<success_criteria>

- Options page exists and matches the compact dark Curst design.
- Provider/default/cost/diagnostic settings can be persisted locally.
- Popup is ready to display LLM session visibility.

</success_criteria>

