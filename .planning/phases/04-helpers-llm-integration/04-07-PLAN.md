---
phase: 04-helpers-llm-integration
plan: 04-07
type: implementation
wave: 6
depends_on: [04-02, 04-03, 04-04, 04-05, 04-06]
requirements: [HLP-01, HLP-05, HLP-06, HLP-08]
files_modified:
  - experiments/andrew/helper-demo/manifest.json
  - experiments/andrew/helper-demo/experiment.ts
  - apps/extension/entrypoints/popup/components/ExperimentRow.tsx
  - apps/extension/entrypoints/popup/components/ExperimentRow.test.tsx
  - apps/extension/src/popup/status.ts
  - apps/extension/src/popup/status.test.ts
autonomous: true
---

<objective>
Add a reference helper experiment and finish popup integration for LLM session visibility and missing-key inline warnings.
</objective>

<tasks>

## Task 1: Reference Helper Experiment

Files:

- `experiments/andrew/helper-demo/manifest.json`
- `experiments/andrew/helper-demo/experiment.ts`

Action:

- Create a ya.ru-scoped isolated-world experiment.
- Use `helpers.injectStyle` and `helpers.injectNode` for its UI.
- Use `helpers.fetchPage` against an SSR-like ya.ru URL where feasible.
- Use `helpers.llm` only when provider key exists; otherwise render a small local fallback/warning.
- Return no manual cleanup beyond relying on helper ledger where possible.

Acceptance criteria:

- Build validates manifest.
- Experiment can be enabled/disabled without leaked DOM nodes.

## Task 2: Popup Inline Missing-Key Warning

Files:

- `apps/extension/entrypoints/popup/components/ExperimentRow.tsx`
- `apps/extension/entrypoints/popup/components/ExperimentRow.test.tsx`
- `apps/extension/src/popup/status.ts`
- `apps/extension/src/popup/status.test.ts`

Action:

- Mark LLM-capable experiments by a simple manifest convention or by last helper error metadata from 04-03.
- If selected/default provider key is missing, show a small inline warning only inside that experiment row.
- Add a compact "Options" action opening `chrome.runtime.openOptionsPage()`.
- Do not show a global banner.

Acceptance criteria:

- Warning appears only for LLM-capable/LLM-attempted experiment rows.
- Non-LLM experiments remain unchanged.

## Task 3: Session Counter Polish

Files:

- `apps/extension/entrypoints/popup/App.tsx`
- `apps/extension/entrypoints/popup/App.test.tsx`

Action:

- Ensure header counter reads calls/tokens from storage changes.
- Style it compactly with existing dark UI.
- Show warning threshold state subtly, without blocking controls.

Acceptance criteria:

- Counter updates after mocked LLM call.
- Existing active experiment count remains visible.

</tasks>

<verification>

- `corepack pnpm vitest run apps/extension/entrypoints/popup/App.test.tsx apps/extension/entrypoints/popup/components/ExperimentRow.test.tsx apps/extension/src/popup/status.test.ts`
- `corepack pnpm typecheck`
- `corepack pnpm -F @platform/extension build`

</verification>

<success_criteria>

- A real reference experiment exercises Phase 4 helpers.
- Missing API key UX is local and non-noisy.
- Popup shows LLM cost visibility without becoming a settings page.

</success_criteria>

