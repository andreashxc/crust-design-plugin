---
phase: 04-helpers-llm-integration
plan: 04-08
type: validation
wave: 7
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07]
requirements: [HLP-01, HLP-02, HLP-03, HLP-04, HLP-05, HLP-06, HLP-07, HLP-08, HLP-09]
files_modified:
  - .planning/phases/04-helpers-llm-integration/04-MANUAL-SMOKE.md
  - .planning/phases/04-helpers-llm-integration/04-08-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
---

<objective>
Validate Phase 4 end-to-end with automated gates, build artifact checks, and a manual Chrome smoke checklist for provider keys, LLM cache/counter behavior, fetchPage, helper cleanup, and streaming.
</objective>

<tasks>

## Task 1: Run Full Automated Gates

Action:

- Run:
  - `corepack pnpm test -- --run`
  - `corepack pnpm typecheck`
  - `corepack pnpm lint`
  - `corepack pnpm check-csp`
  - `corepack pnpm build`

Acceptance criteria:

- All commands exit 0.
- Any known lint warnings are documented.

## Task 2: Capture Build Artifacts

Action:

- Inspect:
  - `apps/extension/.output/chrome-mv3/manifest.json`
  - `apps/extension/.output/chrome-mv3/options.html`
  - `apps/extension/.output/chrome-mv3/registry.json`
  - emitted helper-demo chunk.

Acceptance criteria:

- Manifest includes options page and offscreen permission.
- Registry includes helper-demo.

## Task 3: Write Manual Smoke Checklist

Files:

- `.planning/phases/04-helpers-llm-integration/04-MANUAL-SMOKE.md`

Action:

- Include checks:
  - open options page.
  - save invalid key, test provider failure, verify no secret appears in popup/content state.
  - configure real OpenAI or Anthropic key if available.
  - enable helper demo on ya.ru.
  - call `llm()` once; popup counter increments.
  - call same prompt twice within 10 minutes; second result is cached and counter does not increment network calls.
  - trigger provider failure; error surfaces clearly.
  - run `fetchPage` SSR success and SPA-shell warning.
  - disable helper demo; injected nodes/styles/listeners are cleaned.
  - run/verify simulated long stream or real streaming smoke if key available.

Acceptance criteria:

- Checklist is concrete enough for Andrew to execute locally.

## Task 4: Update GSD State

Files:

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/04-helpers-llm-integration/04-08-SUMMARY.md`

Action:

- Mark Phase 4 plans complete only after automated gates pass.
- Record manual smoke pending or passed.
- Point next up to Phase 5 planning.

Acceptance criteria:

- GSD state no longer says Phase 4 plans are TBD after execution completes.

</tasks>

<verification>

- `corepack pnpm test -- --run`
- `corepack pnpm typecheck`
- `corepack pnpm lint`
- `corepack pnpm check-csp`
- `corepack pnpm build`

</verification>

<success_criteria>

- Phase 4 has automated evidence and a local Chrome smoke checklist.
- LLM helper, fetchPage, cleanup helpers, options page, counters, cache, retry, rate-limit, and streaming are all covered.

</success_criteria>

