---
phase: 05-dx-spa-composition-sharing
plan: 05-06
type: implementation
wave: 3
depends_on: [05-01, 05-04, 05-05]
requirements: [DX-05, SHR-01, SHR-02, DIST-02]
files_modified:
  - docs/ONBOARDING.md
  - docs/EXPERIMENT_AUTHORING.md
  - apps/extension/entrypoints/popup/App.tsx
  - apps/extension/entrypoints/popup/App.test.tsx
  - apps/extension/src/shared/storage.ts
  - apps/extension/src/shared/storage.test.ts
  - apps/extension/src/shared/messages.ts
  - apps/extension/entrypoints/background.ts
  - apps/extension/src/background/handlers/update-check.ts
  - apps/extension/wxt.config.ts
autonomous: true
---

<objective>
Complete self-service onboarding and add a quiet sideload update-available signal.
</objective>

<threat_model>
Threats:
- Medium: broad GitHub host permissions. Mitigation: use the narrowest GitHub API URL pattern needed.
- Low: update check network failure scares users. Mitigation: no banner on failure; diagnostics only.
- Low: docs drift from commands. Mitigation: docs use exact `corepack pnpm` commands from package.json.
</threat_model>

<tasks>

## Task 1: Write Onboarding Docs

Read first:

- `.planning/PROJECT.md`
- `.planning/phases/05-dx-spa-composition-sharing/05-CONTEXT.md`
- `package.json`
- `apps/extension/README.md`

Action:

- Create `docs/ONBOARDING.md` with exact steps:
  - clone repo
  - install dependencies
  - run `corepack pnpm dev`
  - load unpacked `apps/extension/.output/chrome-mv3`
  - create experiment with `corepack pnpm create-experiment <author> <folder> [Display name]`
  - test on `https://ya.ru/`
  - push/pull workflow
- Create `docs/EXPERIMENT_AUTHORING.md` covering manifest, tweaks, helpers, presets, descriptions, and common troubleshooting.

Acceptance criteria:

- `docs/ONBOARDING.md` contains `corepack pnpm dev`.
- `docs/ONBOARDING.md` contains `apps/extension/.output/chrome-mv3`.
- `docs/EXPERIMENT_AUTHORING.md` contains `helpers.llm`, `helpers.fetchPage`, and `presets`.

## Task 2: Improve Popup Empty/Discovery Guidance

Read first:

- `apps/extension/entrypoints/popup/App.tsx`
- `apps/extension/entrypoints/popup/components/EmptyState.tsx`

Action:

- Make empty states explain:
  - registry empty: run `corepack pnpm dev` or build
  - page scope mismatch: current page has no matching experiments
  - search mismatch: no matching text
- Keep copy short; no large help panel.

Acceptance criteria:

- Component tests assert three distinct empty-state messages.

## Task 3: Add Update Check Storage and Background Handler

Read first:

- `apps/extension/src/shared/storage.ts`
- `apps/extension/src/shared/messages.ts`
- `apps/extension/entrypoints/background.ts`
- `apps/extension/wxt.config.ts`

Action:

- Add update state type:
  - current version/commit
  - remote version/commit
  - `available: boolean`
  - `checkedAt`
  - optional error for diagnostics
- Add background handler `UPDATE_CHECK`.
- Check GitHub release or commit endpoint with timeout.
- Add narrow `host_permissions` for the selected GitHub API endpoint.
- Cache result for at least 30 minutes.

Acceptance criteria:

- Unit test: newer remote sets `available: true`.
- Unit test: same remote sets `available: false`.
- Unit test: network failure returns `{ ok: false }` but popup remains quiet.
- `apps/extension/wxt.config.ts` contains a GitHub API host permission only if the handler uses network fetch.

## Task 4: Add Update Banner UI

Read first:

- `apps/extension/entrypoints/popup/App.tsx`
- `.planning/phases/05-dx-spa-composition-sharing/05-UI-SPEC.md`

Action:

- Show compact `Update available` banner only when update state says available.
- Include `View` action that opens the GitHub release/commit URL.
- Do not show banner for unknown/error state.

Acceptance criteria:

- Component test shows `Update available` for available state.
- Component test does not show update text for error/unknown state.

</tasks>

<verification>

- `corepack pnpm vitest run apps/extension/src/shared/storage.test.ts apps/extension/entrypoints/popup/App.test.tsx apps/extension/src/background/**/*.test.ts`
- `corepack pnpm typecheck`
- `corepack pnpm build`

</verification>

<success_criteria>

- A new designer can self-onboard from docs.
- Pulled experiments have clear discovery guidance.
- Sideload users see a quiet update signal only when it is actionable.

</success_criteria>

