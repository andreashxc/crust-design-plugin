---
phase: 05-dx-spa-composition-sharing
plan: 05-07
type: validation
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06]
requirements: [ENG-07, ENG-08, ENG-09, DX-01, DX-02, DX-03, DX-04, DX-05, TWK-08, UI-06, UI-07, SHR-01, SHR-02, SHR-04, DIST-02]
files_modified:
  - .planning/phases/05-dx-spa-composition-sharing/05-MANUAL-SMOKE.md
  - .planning/phases/05-dx-spa-composition-sharing/05-07-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
---

<objective>
Validate Phase 5 end-to-end with automated gates, build artifact checks, and manual Chrome smoke for hot reapply, SPA navigation, composition order, presets, onboarding, and update banner.
</objective>

<threat_model>
Threats:
- Medium: automated tests pass but dev hot-reapply fails in real browser. Mitigation: manual smoke requires browser timing check under `pnpm dev`.
- Medium: preset save path appears to work but no file lands in repo. Mitigation: manual smoke checks file existence.
- Low: update banner depends on live GitHub network. Mitigation: automated mocked test plus optional live smoke.
</threat_model>

<tasks>

## Task 1: Run Full Automated Gates

Read first:

- `.planning/phases/05-dx-spa-composition-sharing/05-VALIDATION.md`

Action:

- Run:
  - `corepack pnpm test -- --run`
  - `corepack pnpm typecheck`
  - `corepack pnpm lint`
  - `corepack pnpm check-csp`
  - `corepack pnpm build`

Acceptance criteria:

- All commands exit 0.
- Known lint warnings, if any remain, are documented in `05-07-SUMMARY.md`.

## Task 2: Capture Build Artifacts

Read first:

- `apps/extension/.output/chrome-mv3/manifest.json`
- `apps/extension/.output/chrome-mv3/registry.json`

Action:

- Inspect built manifest and registry.
- Confirm registry includes Phase 5 metadata.
- Confirm manifest permissions remain narrow.

Acceptance criteria:

- `registry.json` includes at least one `descriptionStatus`.
- `registry.json` includes `presets` or an empty preset metadata shape.
- Manifest contains no broad `https://*/*` permission added for update checks.

## Task 3: Write Manual Smoke Checklist

Read first:

- `.planning/phases/05-dx-spa-composition-sharing/05-VALIDATION.md`

Action:

- Create `05-MANUAL-SMOKE.md` with the ten smoke checks from `05-VALIDATION.md`.
- Include exact browser/dev-server setup:
  - `corepack pnpm dev`
  - load unpacked path
  - `https://ya.ru/`
- Include pass/fail columns.

Acceptance criteria:

- `05-MANUAL-SMOKE.md` contains `<2 seconds`.
- `05-MANUAL-SMOKE.md` contains `pushState`.
- `05-MANUAL-SMOKE.md` contains `Toolbar badge`.
- `05-MANUAL-SMOKE.md` contains `Preset file exists`.

## Task 4: Update GSD State

Read first:

- `.planning/ROADMAP.md`
- `.planning/STATE.md`

Action:

- Mark Phase 5 plans complete after automated validation.
- Set next focus to Phase 6 planning after manual smoke is recorded or explicitly pending.
- Write `05-07-SUMMARY.md` with changed files, test output summary, and manual smoke status.

Acceptance criteria:

- `.planning/STATE.md` current focus references Phase 6 only after Phase 5 validation work is complete.
- `.planning/ROADMAP.md` Phase 5 status reflects complete or manual-smoke-pending accurately.

</tasks>

<verification>

- `corepack pnpm test -- --run`
- `corepack pnpm typecheck`
- `corepack pnpm lint`
- `corepack pnpm check-csp`
- `corepack pnpm build`

</verification>

<success_criteria>

- Phase 5 has automated evidence and a manual browser smoke checklist.
- All Phase 5 requirements are covered by at least one plan and validation artifact.

</success_criteria>

