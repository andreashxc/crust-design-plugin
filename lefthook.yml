pre-commit:
  parallel: true
  commands:
    lint:
      run: corepack pnpm lint
    typecheck:
      run: corepack pnpm typecheck
    test:
      run: corepack pnpm test --run
    check-csp:
      run: corepack pnpm check-csp
