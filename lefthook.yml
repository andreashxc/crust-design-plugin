pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck
    test:
      run: pnpm test --run
    # check-csp: temporarily disabled — scripts/check-csp.ts is shipped in Plan 01-05.
    # Plan 01-05 MUST restore the original entry verbatim:
    #   check-csp:
    #     run: pnpm check-csp
